const initialState = {
  UserProfiles: []
};

const UserProfileReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default UserProfileReducer;
